using UnityEngine;

namespace MapChunkSystem2D
{
    public class DataInitialize : MonoBehaviour
    {
        public virtual void Initialize(IntDelegate chunksLoadCountXDelegate, IntDelegate chunksLoadCountYDelegate, IntDelegate sizeXDelegate, IntDelegate sizeYDelegate)
        {

        }
    }
}